import { BgHighLightDirective } from './bg-high-light.directive';

describe('BgHighLightDirective', () => {
  it('should create an instance', () => {
    const directive = new BgHighLightDirective();
    expect(directive).toBeTruthy();
  });
});
